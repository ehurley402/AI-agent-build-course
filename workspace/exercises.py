"""
Lesson A3 — Programming basics.

Fill in the four functions below, one at a time, in order. Each one introduces
one idea. Leave the function names and parameter names exactly as they are —
the automated checks look for them by name.
"""


def greet(name):
    """Return the string "Hello, <name>!" — e.g. greet("Ada") -> "Hello, Ada!"

    TODO: replace `pass` with a return statement.
    """
    pass


def make_person(name, age):
    """Return a dictionary with two keys, "name" and "age", holding the values
    passed in — e.g. make_person("Ada", 30) -> {"name": "Ada", "age": 30}

    TODO: replace `pass` with a return statement.
    """
    pass


def to_json_string(data):
    """Convert the `data` dictionary into a JSON-formatted string and return it.

    Hint: Python's built-in `json` module has a function for exactly this —
    add `import json` at the top of the file, then look up `json.dumps`.

    TODO: replace `pass` with a return statement.
    """
    pass


def count_vowels(word):
    """Return how many of the letters a, e, i, o, u appear in `word`
    (lowercase only — you can assume `word` is already lowercase).

    e.g. count_vowels("hello") -> 2

    TODO: replace `pass` with code that loops over the letters in `word` and
    counts the vowels, then returns the count.
    """
    pass
